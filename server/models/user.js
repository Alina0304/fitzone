/*const mysql = require('mysql');
let schema= {
    const sqlInsert = "INSERT INTO fit.auth (email, pass) VALUES (?,?)";
    db.query(sqlInsert, [FIO_cl, Age], (err, result) => {
        console.log(result);
    });

}
model.exports = model('User', schema)*/